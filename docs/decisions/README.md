# Journal des décisions d'architecture (ADR)

Une décision importante, une conséquence durable = un fichier.
Format : contexte → décision → conséquences → alternatives écartées.
Une décision ne se supprime pas : elle est remplacée par une nouvelle qui la
référence (statut `Remplacée par ADR-NNN`).
